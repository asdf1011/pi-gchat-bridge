{
  "name": "pi-gchat-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Bridge between Google Chat and the pi coding agent",
  "scripts": {
    "start": "tsx src/index.ts",
    "dev": "tsx watch src/index.ts",
    "typecheck": "tsc --noEmit",
    "build": "tsc"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "^0.84.1",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.0"
  }
}
